#include <stdio.h>
#include <stdlib.h>
#include <unistd.h>
#include <time.h>

int main(int argc, char ** argv) {


	if(argc !=2){
	fprintf(stderr, "Aufruf: %s Integer Wert", argv[0]);
	}

	printf("helloMechanismTask4.c:\n");
	printf("	My PID %d.\n", getpid());

	long n = atoi(argv[1]);
	struct timespec start, end, start2,end2;
	long result = 0;

	clock_gettime(CLOCK_REALTIME, &start);
	for(int i = 0; i < n; ++i)
		getpid();
	}
	clock_gettime(CLOCK_REALTIME, &end);

	clock_gettime(CLOCK_REALTIME, &start2);
	for(int i = 0; i < n; ++i){
		clock_gettime(CLOCK_REALTIME, &end2);
	}
	result = ((end.tv_sec - start.tv_sec)*1000000000 + (end.tv_nsec - start.tv_nsec) - (end2.tv_sec - start2.tv_sec)*1000000000 + end2.tv_nsec - start2.tv_nsec);

	printf("duration in nanoseconds: %ld\n", result/n);

return 0;
}
